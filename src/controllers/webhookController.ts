/*import { Request, Response } from 'express';

export const handleWebhook = (req: Request, res: Response) => {
  try {
    // Implementa lógica para manejar webhooks
    const webhookData = req.body;  // Ajusta según la estructura de tus webhooks
    console.log('Webhook recibido:', webhookData);
    res.status(200).send('Webhook recibido correctamente');
  } catch (error) {
    console.error(error);
    res.status(500).send('Error al manejar el webhook');
  }
};*/
